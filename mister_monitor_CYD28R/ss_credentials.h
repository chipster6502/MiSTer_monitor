// ss_credentials.h
// When empty (source builds / forks), the
// firmware falls back to ss_dev_user / ss_dev_pass from config.ini.
#pragma once

#define SS_DEV_ID_EMBEDDED   ""
#define SS_DEV_PASS_EMBEDDED ""